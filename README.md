# 🔐 Symfony Authentication Boilerplate

A modern Symfony authentication starter with **Login**, **Registration**, and **Google OAuth2 Sign-In** support — built for speed and simplicity.

---

## 🚀 Features

- 🧑‍💻 User registration & login (Symfony Security)
- 🔑 OAuth2 login with Google (via `knpuniversity/oauth2-client-bundle`)
- 🔒 Password hashing with Symfony's security encoder
- 🧱 Doctrine ORM & migrations ready
- 🧭 Configurable redirect after login
- 🧰 Ready for extension (roles, profile data, etc.)

---

## 🧩 Requirements

- PHP 8.2+
- Composer
- Symfony CLI (recommended)
- MySQL or PostgreSQL
- Google Cloud account (for OAuth2)

---

## ⚙️ Installation

Clone the repo and install dependencies:

```bash
git clone https://github.com/yourusername/your-repo-name.git
cd your-repo-name
composer install
